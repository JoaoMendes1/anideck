# Arquivos de ambiente
.env*

# Binários do Go
/bin/
/anideck

# Pastas e arquivos do SO
.DS_Store
Thumbs.db

/client/dist/
/client/node_modules/supabase/.temp/

# CLI do Supabase — artefatos locais (.temp, .branches) e config gerado.
# A pasta inteira fica de fora por escolha: é um projeto de um dev só, e o
# config.toml é regenerável com `supabase init`.
supabase/

# Backups locais — contêm dado de usuário, repo é público
*.backup.sql
.env.backup
backup_capas/
sql/*local.sql